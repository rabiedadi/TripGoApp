import { NgForCountPipe } from './ng-for-count.pipe';

describe('NgForCountPipe', () => {
  it('create an instance', () => {
    const pipe = new NgForCountPipe();
    expect(pipe).toBeTruthy();
  });
});
